server {
  listen 80 default_server;
  listen [::]:80 default_server;

  server_name _;

  access_log  /var/log/nginx/http.access.log ltsv;
  error_log   /var/log/nginx/http.error.log;

  location / {
      return 301 https://$host$request_uri;
  }
}
